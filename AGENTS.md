# Codex 個人化規則

- Always use Traditional Chinese to answer the question.
- Git commit messages must follow the existing project style; when no more specific rule exists, use the Conventional Commit format, such as `feat: ...`, `fix: ...`, or `docs: ...`.
- Before committing, inspect recent commit messages and the staged diff, and do not use a plain English sentence as the commit message.
